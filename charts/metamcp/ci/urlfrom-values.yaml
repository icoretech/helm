users:
  - email: admin@example.com
    name: Admin
    password: change-me

provision:
  enabled: true
  servers:
    - name: automation-remote
      type: SSE
      urlFrom:
        - secretRef:
            name: metamcp-remote-url
