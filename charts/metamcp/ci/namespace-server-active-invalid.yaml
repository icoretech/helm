auth:
  betterAuthSecret: dev-secret

users:
  - email: admin@example.com
    name: Admin
    password: changeme

provision:
  enabled: true
  servers:
    - name: billing-sandbox
      type: STDIO
      command: "npx"
  namespaces:
    - name: workspace
      servers:
        - active: false
