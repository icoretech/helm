1. MetaMCP URL

   http://{{ include "metamcp.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

   Endpoints (by namespace/prefix configured in MetaMCP):
   - /metamcp/<name>/sse
   - /metamcp/<name>/mcp
   - /metamcp/<name>/api

2. Postgres

   {{- if .Values.postgres.enabled }}
   A Postgres instance is deployed as {{ include "metamcp.postgresFullname" . }} on port 5432.
   DATABASE_URL is injected into the MetaMCP pod.
   {{- else }}
   Postgres is disabled. Ensure the required DB env vars are provided via values.env/values.secretEnv.
   {{- end }}
