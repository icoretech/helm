# Values used by chart-testing (ct install) to ensure the chart can be installed.
backend:
  config:
    jwtSecret: "replace-with-a-strong-secret-at-least-32-characters"
    frontendOrigin: http://multica.localhost
  email:
    resendFromEmail: noreply@example.com
frontend:
  backendServiceAlias:
    enabled: true
postgres:
  persistence:
    enabled: false
storage:
  local:
    persistence:
      enabled: false
