## Example: Register an external OpenAPI spec with Unla (no Pod created)
servers:
  - name: petstore
    register:
      enabled: true
      type: openapi
      prefix: "/gateway/petstore"
    openapi:
      url: "https://petstore3.swagger.io/api/v3/openapi.json"

