mode: node
node:
  image: node:24-alpine
  package: mcp-remote
  version: latest
  args:
    - https://docs.mcp.cloudflare.com/sse
    - --port
    - "3000"
container:
  port: 3000
service:
  port: 3000
