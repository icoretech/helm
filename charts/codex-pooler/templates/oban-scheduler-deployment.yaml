{{- if .Values.oban.scheduler.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "codex-pooler.fullname" . }}-oban-scheduler
  labels:
    {{- include "codex-pooler.labels" . | nindent 4 }}
    app.kubernetes.io/component: oban-scheduler
spec:
  replicas: {{ .Values.oban.scheduler.replicaCount }}
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: {{ .Values.oban.scheduler.strategy.maxUnavailable }}
      maxSurge: {{ .Values.oban.scheduler.strategy.maxSurge }}
  selector:
    matchLabels:
      {{- include "codex-pooler.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: oban-scheduler
  template:
    metadata:
      labels:
        {{- include "codex-pooler.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: oban-scheduler
        {{- if and .Values.clustering.enabled .Values.clustering.participants.scheduler }}
        {{- include "codex-pooler.clusteringMemberLabel" . | nindent 8 }}
        {{- end }}
      {{- $runtimePodAnnotations := include "codex-pooler.runtimePodAnnotations" (dict "root" . "podAnnotations" .Values.oban.scheduler.podAnnotations) | trim }}
      {{- if $runtimePodAnnotations }}
{{ $runtimePodAnnotations | nindent 6 }}
      {{- end }}
    spec:
      {{- if and .Values.clustering.enabled .Values.clustering.participants.scheduler }}
      subdomain: {{ include "codex-pooler.clusteringHeadlessServiceName" . }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.oban.scheduler.terminationGracePeriodSeconds }}
      serviceAccountName: {{ include "codex-pooler.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: oban-scheduler
          image: {{ include "codex-pooler.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if and .Values.clustering.enabled .Values.clustering.participants.scheduler }}
          ports:
            - name: epmd
              containerPort: {{ .Values.clustering.epmdPort }}
            - name: distribution
              containerPort: {{ .Values.clustering.distributionPort }}
          {{- end }}
          env:
            - name: OBAN_MODE
              value: scheduler
            {{- include "codex-pooler.clusteringEnv" (dict "root" . "role" "scheduler") | nindent 12 }}
            {{- include "codex-pooler.env" . | nindent 12 }}
          # `rollout status` on these Deployments used to mean nothing: with no
          # probe at all, a worker reported Ready against a database with no
          # schema. Readiness deliberately, not startup or liveness — restarting
          # a container cannot apply a migration, so those would crash-loop.
          # `rpc` spawns a BEAM per call, hence the slow cadence and the
          # generous timeout; `ERL_AFLAGS=` clears inherited flags the same way
          # the app's preStop command does. `readiness_check/0` is a
          # cross-repository contract like `prepare_shutdown/1`, and the
          # `function_exported?` wrapper keeps images that predate it Ready
          # instead of never becoming Ready at all.
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -ec
                - 'ERL_AFLAGS= timeout 9s /app/bin/codex_pooler rpc "if function_exported?(CodexPooler.Release, :readiness_check, 0), do: CodexPooler.Release.readiness_check(), else: :ok"'
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          resources:
            {{- toYaml .Values.oban.scheduler.resources | nindent 12 }}
      {{- with .Values.oban.scheduler.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.oban.scheduler.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.oban.scheduler.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
